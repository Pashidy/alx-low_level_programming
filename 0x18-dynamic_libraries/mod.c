#include <stdlib.h>
#include <stdio.h>

/**
 * mod - module of two integers
 * @a: first int
 * @b: second int
 * Return: module of both integers
 */
int mod(int a, int b)
{       
        return (a % b);
}
